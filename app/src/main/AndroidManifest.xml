<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arm_market_firebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ARM_Market_Firebase">
        <activity
            android:name=".ProductList"
            android:exported="false"
            android:theme="@style/Theme.ARM_Market_Firebase.NoActionBar"/>
        <activity
            android:name=".ProdElement"
            android:exported="false"
            android:label="@string/title_activity_prod_type"
            android:theme="@style/Theme.ARM_Market_Firebase.NoActionBar"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ARM_Market_Firebase.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>